#include <stdio.h> 
int main() 
{ 
int firstno, secondno, *ptr1, *ptr2, sum;

printf(" enter the first number: "); 
scanf("%d", &firstno);
 printf("enter the second number: ");
 scanf("%d", &secondno);
ptr1= &firstno;
 ptr2= &secondno; 
sum = *ptr1+*ptr2; 
printf("sum=%d",sum);
 return 0; 
}
